#include<stdio.h>
#include<conio.h>
main()
{
	char university[100];
	printf("Enter university: ");
	gets(university);
	printf("Your university is %s", university);
	getch();
	return 0;
}

